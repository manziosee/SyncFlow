defmodule SyncFlow.Core.Router do
  # Router moved to SyncFlow.Web.CommandedRouter (compiled after all domain apps).
  # This stub keeps the module name alive for any existing references.
  use Commanded.Commands.Router
end
